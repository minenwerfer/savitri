//import webpack from 'webpack'
//import { iconNames } from './iconLoader'

//type Options = {
//  //
//}

//const plugin = (options?: Options) => ({
//  apply: (compiler: webpack.Compiler) => {
//    compiler.hooks.afterEmit.tap('SvIcon', (compilation) => {
//      const names = [ ...iconNames ].join('\n')
//      compilation.emitAsset('test.txt', new webpack.sources.RawSource(names))
//    })
//  }
//})

//export default plugin
//
export default 'oi'
